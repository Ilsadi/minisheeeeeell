// static void	handle_fd_duplications(t_pipex *p, t_token *tokens)
// {
// 	if (!has_input_redirection(tokens))
// 	{
// 		if (p->fd_in < 0 || dup2(p->fd_in, STDIN_FILENO) == -1)
// 			(close_all(p), exit(1));
// 	}
// 	if (!has_output_redirection(tokens))
// 	{
// 		if (p->fd_out < 0 || dup2(p->fd_out, STDOUT_FILENO) == -1)
// 			(close_all(p), exit(1));
// 	}
// }

// static void	free_and_exit(t_child_ctx *ctx, int status)
// {
// 	ft_free_tab(ctx->envp);
// 	close_all(ctx->p);
// 	destroy_tab(ctx->mini->env);
// 	rb_free_all(ctx->mini->rb);
// 	free(ctx->mini->rb);
// 	exit(status);
// }

// static void	handle_empty_cmd(t_child_ctx *ctx, int redir_only)
// {
// 	if (redir_only)
// 		free_and_exit(ctx, 0);
// 	else
// 	{
// 		close_all(ctx->p);
// 		ft_free_tab(ctx->envp);
// 		ft_error_exit("Pipex: command not found\n");
// 	}
// }

